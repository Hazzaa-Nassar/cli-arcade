# cli-arcade

A tiny terminal arcade (Python stdlib only).

## Run

python -m cli_arcade
python -m cli_arcade list
python -m cli_arcade play guess_number
python -m cli_arcade stats

## Progress storage
Saved locally to:
- ~/.cli_arcade/progress.json
